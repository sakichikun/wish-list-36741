<div class="contents row">
  <div class="content_post">
    <p><%= @want.title %></p>
    <span class="name">
      <%= @want.name %>
    </span>
    <%= link_to '編集', edit_want_path(@want.id), method: :get %>
    <%= link_to '削除', want_path(@want.id), method: :delete %>
    <%= link_to '戻る', wants_path %>
  </div>
</div>